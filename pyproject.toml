[project]
name = "gnmi-debug-demo"
version = "0.1.0"
description = "gNMI interface demo for Video 3 — Debug Python the Right Way"
requires-python = ">=3.10"
dependencies = [
    "pygnmi>=0.8.14",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]
